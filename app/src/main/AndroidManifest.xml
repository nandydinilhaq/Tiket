<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dinil.ticket">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".tiket_detail"></activity>
        <activity android:name=".ganti_password" />
        <activity android:name=".edit_profil" />
        <activity android:name=".profil" />
        <activity android:name=".sukses_beli" />
        <activity android:name=".menu_destinasi" />
        <activity android:name=".tiket_checkout" />
        <activity android:name=".monas_detail" />
        <activity android:name=".sukses_registrasi" />
        <activity android:name=".home" />
        <activity android:name=".registrasi1" />
        <activity android:name=".login" />
        <activity android:name=".getstarted" />
        <activity android:name=".splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>